//! `weft-clustermgr` operator entry point (skeleton).
//!
//! Once `kube-rs` lands, this runs the reconcile controller watching `WeftCluster` resources.

fn main() {
    println!(
        "weft-clustermgr: WeftCluster operator (skeleton). Reconcile loop pending kube-rs deps."
    );
}
