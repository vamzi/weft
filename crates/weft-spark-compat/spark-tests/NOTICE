The files under `inputs/` and `results/` in this directory are vendored verbatim from
Apache Spark:

    github.com/apache/spark
    path:  sql/core/src/test/resources/sql-tests/{inputs,results}
    tag:   v4.0.0   (see VERSION for the exact commit SHA)

Apache Spark is licensed under the Apache License, Version 2.0. These files are
redistributed here, unmodified, under that same license solely to measure Weft's SQL
semantic parity against Spark's own reference outputs. No endorsement by the Apache
Software Foundation is implied.

The `results/*.sql.out` files are Spark's authoritative expected outputs (generated by
Spark's `SQLQueryTestSuite` with `SPARK_GENERATE_GOLDEN_FILES=1`). Weft treats them as
the oracle: a query "passes" when weft's Spark-formatted output matches the golden block.
