//! `weft-scheduler` entry point (skeleton). Polls the Postgres job queue once deps are wired.

fn main() {
    println!(
        "weft-scheduler: cron + DAG executor (skeleton). Queue/executor pending sqlx+tokio deps."
    );
}
